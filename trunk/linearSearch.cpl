$ PL Program: Linear Search?
begin

  const n = 10; 

  integer array A [n];
  integer x, i;
      if A[3] = x -> i := i + 1; [] 
	~(A[3] = x) -> i := i + 1; []
	A[3] = x -> i := i + 1;
      fi;

end.

$todo add in boolean or integer print out for multiple declaration error